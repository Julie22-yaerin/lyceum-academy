fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
pydantic-settings>=2.5.0
pyjwt>=2.9.0
cryptography==48.0.1
httpx>=0.27.0
# Admin — RAG + fine-tuning
torch>=2.0.0          # required by sentence-transformers (CPU/MPS, macOS-compatible)
chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers<5                # transformers 5.x breaks sentence-transformers' internal imports (NameError: 'nn' not defined)
pymupdf>=1.24.0          # replaces PyPDF2 — faster, better layout, can render pages to image
python-multipart>=0.0.9
Pillow>=10.0.0
pytesseract>=0.3.10
# OpenAI fine-tuning pipeline
openai>=1.0.0
# Rate limiting
slowapi>=0.1.9
# WebSocket proxy for Gemini Live (S2S voice)
websockets>=12.0
cryptography>=41.0.0
# Firebase Admin SDK — Firestore personas storage
firebase-admin==6.5.0
# Stripe payments (v2)
stripe>=12.0.0

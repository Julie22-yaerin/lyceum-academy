// Re-export kept for backwards compatibility. The canonical definition now
// lives in `lib/topic-map.ts` (moved out of the page file, which can't export
// non-page values under the Next.js App Router).
export { NODE_COLORS } from "@/lib/topic-map";
export type { MapNode } from "@/lib/topic-map";
